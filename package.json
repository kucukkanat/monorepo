{
  "name": "monorepo",
  "version": "1.0.0",
  "private": true,
  "main": "index.js",
  "workspaces": ["packages/**"],
  "scripts": {
    "dev": "lerna run dev --parallel",
    "build": "lerna run build --parallel",
    "start": "lerna run start --parallel"
  },
  "license": "MIT",
  "dependencies": {
    "lerna": "^3.20.2"
  }
}
